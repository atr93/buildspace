# AdminOpsDashboard.Mvc

ASP.NET Core MVC (server-rendered) parallel version of the Admin Analytics Dashboard.

## Run
```bash
dotnet restore
dotnet run
```

Open `https://localhost:5001` (or printed URL).
